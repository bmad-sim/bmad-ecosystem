mat_det -> function.

writeup bpm_x_gain, etc.

a0/b0 -> a_1/b_1

Put RF on warning in 
  b) twiss_propagate1  ?!
  c) twiss_at_start    ?!

Structure name changes:
	bmad_com  --> bmad_parameters
	etc...

Better bmad digested file compression: 
  compress ele%value array.
  compress names.

Enable overlay to control:
	aperture
	x_limit
	y_limit

Put in mode_info_struct:
	sigma
	simgap
and enable reading in sigmas in bmad_parser & xsif_parser

emittance, emitt -> emit
[Remember bmadz, etc.]

pointers -> allocatable in ele_struct, lat_struct, etc.

-----------------

add function to calc: ring%param%s_start
add function to calc: ring%param%s_end

